mixin language-list(props = {})
  include language

  ul.language-list&attributes(attributes)
    li.language-list__item
      +language({lang: 'eu'})
    li.language-list__item
      +language({lang: 'de'})
